import React from "react";

const Splash = () => 
    <p>Need to gather images from HEB and make a nice card with the image and the store name underneath. use map to attach state to each one, state should be certain info from database i.e. name, id</p>;

export default Splash;